/** Host entry for the browser-only companion; contributes no model or session behavior. */

/** Register the package for browser discovery without host-side effects. */
export function apply(): void {}
